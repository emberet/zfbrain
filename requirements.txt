# Everything the repo actually imports.
#
# brain + roamer
numpy>=1.26
pandas>=2.0            # build_graph.py (CSV -> graph.npz)
playwright>=1.45       # roam.py — then run: python -m playwright install chromium
pillow>=10.0           # roam.py — decodes the screenshot for the retina + live frame
#
# solana launch rig (solkeygen / soldryrun / sollive / solrpc)
solders>=0.18
base58>=2.1
requests>=2.31
#
# narrator (voice.py) — Anthropic SDK; needs ANTHROPIC_API_KEY in .env
anthropic>=0.40
#
# data spike only (fetch_cave.py); not needed to run the fish
# caveclient
